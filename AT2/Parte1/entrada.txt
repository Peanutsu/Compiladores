// Programa de teste para o analisador lexico de Rust
/* Este e um comentario
   de bloco com varias linhas */

fn main() {
    let idade: i32 = 25;
    let altura: f64 = 1.75;
    let nome: &str = "Maria";
    let letra: char = 'A';
    let mut soma = 0;
    let ativo = true;

    if idade >= 18 && ativo {
        soma = soma + idade * 2;
    } else {
        soma -= 1;
    }

    let vetor = [1, 2, 3, 4, 5];
    let fatia = &vetor[1..3];

    for i in 0..10 {
        soma += i;
    }

    let ponteiro_valor: u32 = 0xFF;
    let binario = 0b1010;
    let grande = 1_000_000;

    println!("Soma final: {}", soma);

    // string nao fechada para testar erro lexico
    let erro_string = "isso nao fecha

    let simbolo_invalido = 3 ~ 4;
}